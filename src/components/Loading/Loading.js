import React from 'react'
import loadingImg from '../../loading.gif'

const loading = () => {
    return (
        <div>
            <img src={loadingImg} />
        </div>
    )
}

export default loading